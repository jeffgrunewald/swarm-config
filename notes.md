Config Examples
===============

{
    "cluster-name": {
        "host": "hostname-or-ip",
        "port": portNum,
        "tls": bool,
        "cert_path": "path-to-certs"
    }
}

Commands:
swarm-config write-config

swarm-config set-context

swarm-config del-config
